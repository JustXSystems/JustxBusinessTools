GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=change-me-now
GF_SERVER_ROOT_URL=https://justxsystems.com/grafana/
GF_SERVER_DOMAIN=justxsystems.com

# Optional MySQL metrics (docker compose --profile mysql-metrics up -d)
# Create a read-only user first, then:
# MYSQL_EXPORTER_DSN=jbt_exporter:SECRET@(host.docker.internal:3306)/
# MYSQL_EXPORTER_DSN=
